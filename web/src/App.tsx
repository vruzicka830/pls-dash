// Placeholder — proves the toolchain (Vite/React/TS build) works end to
// end before any real UI exists. Delete once the actual dashboard shell
// lands.
function App() {
  return <h1>pls-dash</h1>
}

export default App
